name: Deploy Compliance Agent

on:
  push:
    branches:
      - main
    paths:
      - 'agents/compliance-agent/**'
      - '.github/workflows/deploy-compliance-agent.yml'

jobs:
  deploy:
    runs-on: ubuntu-latest
    
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '18'

      - name: Install Vercel CLI
        run: npm install --global vercel@latest

      - name: Pull Vercel Environment Information
        run: vercel pull --yes --environment=production --token=${{ secrets.VERCEL_TOKEN }}
        working-directory: ./agents/compliance-agent

      - name: Build Project Artifacts
        run: vercel build --prod --token=${{ secrets.VERCEL_TOKEN }}
        working-directory: ./agents/compliance-agent

      - name: Deploy to Vercel
        run: vercel deploy --prebuilt --prod --token=${{ secrets.VERCEL_TOKEN }}
        working-directory: ./agents/compliance-agent
        env:
          VERCEL_ORG_ID: ${{ secrets.VERCEL_ORG_ID }}
          VERCEL_PROJECT_ID: ${{ secrets.VERCEL_COMPLIANCE_PROJECT_ID }}

      - name: Verify Deployment
        run: |
          echo "✅ Compliance Agent deployed successfully!"
          echo "🔗 Check status at: https://vercel.com/dashboard"
